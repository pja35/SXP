In file src/main/java/protocol/impl/sigma/Utils.java

In function rand, defined line 35.

This function is supposed to return a random BigInteger whose value is in [1,p]
Actually it return a random BigInteger.

Problem ::
Line 39 : `while(s.compareTo(BigInteger.ONE)<=0 && s.compareTo(p)>= 0)`

Suggested solution ::
Line 39 : `while(s.compareTo(BigInteger.ONE) < 0 || s.compareTo(p) > 0)`
